fin movement:
	if moving, dont apply friection
	if not moving, apply friction
	
	apply friection:
		if direction x = 0;
			velocity x becomes 0
		if direction y = 0;
			velocity y becomes 0


Action menu:
	WASD to choose between actions
	create timers array where each timer is asscociated with an action
	
	send the timers array to the char
	char will start the timer when action is performed


dash attack:
	create a hit box 
	when enters the hurtbox 
		apply damage


boss attack:
	idle state and attack state
	when idle, start timer, when it's up, choose an attack
	attack array, choose at random
